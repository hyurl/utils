import { until as _until } from "https://lib.deno.dev/x/ayonli_jsext@latest/promise/index.ts";

/** @deprecated use `Promise.until` from `@ayonli/jsext/promise/augment` instead. */
const until = _until;
export default until;
