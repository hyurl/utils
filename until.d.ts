import { until as _until } from "@ayonli/jsext/promise";
/** @deprecated use `Promise.until` from `@ayonli/jsext/promise/augment` instead. */
declare const until: typeof _until;
export default until;
