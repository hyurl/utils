import { random } from "https://lib.deno.dev/x/ayonli_jsext@latest/number/index.ts";

/** @deprecated use `Number.random` from `@ayonli/jsext/number/augment` instead. */
const rand = random;
export default rand;
