import { random } from "https://deno.land/x/ayonli_jsext/number/index.ts";

/** @deprecated use `Number.random` from `@ayonli/jsext/number/augment` instead. */
const rand = random;
export default rand;
