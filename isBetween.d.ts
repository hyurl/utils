/**
 * Checks if a number is between two numbers (gte than the minimal value and lte
 * than the maximum value).
 */
export default function isBetween(value: number, [min, max]: [number, number]): boolean;
