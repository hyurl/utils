/**
 * Check if the target is a real object, that means it's of type `object`, but
 * not an array-like, not a Collection-like, and not a Promise-like object.
 */
export default function isRealObject(target: any): boolean;
